import { useEffect, useMemo, useState, type FormEvent } from 'react';
import { Link } from 'react-router-dom';
import {
  ArrowRight, ArrowUpRight, Award, BookOpen, CalendarDays, Check, ChevronDown, Clock3, Heart,
  Instagram, Linkedin, Mail, MapPin, Menu, MessageCircle, MoveRight, Play, ShieldCheck, Sparkles,
  Users, Video, X, Youtube,
} from 'lucide-react';
const logo = '/src/assets/logos/1_(1).png';
import { contactConfig } from '@/config/contact';
import { courses, images, professionals } from '@/data/homeData';
import { translations } from '@/i18n/translations';
import { useSiteLanguage } from '@/context/SiteLanguageContext';

const navIds = ['top', 'services', 'professionals', 'courses', 'resources', 'contact'];
const navRoutes: Record<number, string> = { 1: '/servicios', 2: '/profesionales', 3: '/cursos', 4: '/recursos', 5: '/contacto' };
const serviceIcons = [Heart, Users, Sparkles];
const trustIcons = [ShieldCheck, MessageCircle, Award, Video];
const benefitIcons = [ShieldCheck, Heart, Award, Sparkles, ShieldCheck];

function SectionHeading({ label, title, text, light = false, action }: { label: string; title: string; text?: string; light?: boolean; action?: string }) {
  return <div className="max-w-2xl">
    <span className={`eyebrow ${light ? 'text-brand-200' : ''}`}>{label}</span>
    <h2 className={`mt-4 text-4xl font-semibold tracking-tight sm:text-5xl ${light ? 'text-white' : 'text-ink'}`}>{title}</h2>
    {text && <p className={`mt-5 max-w-xl text-base leading-7 ${light ? 'text-white/70' : 'text-ink/65'}`}>{text}</p>}
    {action && <a href="#services" className={`group mt-6 inline-flex items-center gap-2 text-sm font-bold ${light ? 'text-white' : 'text-brand-600'}`}>{action}<ArrowRight className="transition-transform group-hover:translate-x-1" size={16} /></a>}
  </div>;
}

function AppButton({ children, href = '#contact', light = false, outline = false, onClick, className = '' }: { children: React.ReactNode; href?: string; light?: boolean; outline?: boolean; onClick?: () => void; className?: string }) {
  const cls = `${className} focus-ring inline-flex min-h-12 items-center justify-center gap-3 rounded-full px-6 text-sm font-bold transition duration-300 hover:-translate-y-1 ${light ? 'bg-white text-brand-700 shadow-soft hover:bg-brand-50' : outline ? 'border border-brand-300 bg-white/50 text-brand-700 hover:border-brand-500 hover:bg-white' : 'bg-brand-gradient text-white shadow-[0_12px_28px_rgba(93,131,167,.28)] hover:shadow-lift'}`;
  // Los enlaces internos (que empiezan con "/") navegan con React Router en
  // vez de recargar la página completa, como sí conviene para los anclas
  // "#contact"/"#services" que solo hacen scroll dentro del Home.
  if (href.startsWith('/')) {
    return <Link to={href} onClick={onClick} className={cls}>{children}</Link>;
  }
  return <a onClick={onClick} href={href} className={cls}>{children}</a>;
}

export default function HomePage() {
  const { language, setLanguage } = useSiteLanguage();
  const [menuOpen, setMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const [active, setActive] = useState('top');
  const [form, setForm] = useState({ name: '', email: '', subject: '', body: '', privacy: false });
  const [submitted, setSubmitted] = useState(false);
  const [sending, setSending] = useState(false);
  
  // Estado para los datos conectados a Supabase (con fallback a mocks)
  const [dbProfessionals, setDbProfessionals] = useState(professionals);
  const [dbCourses, setDbCourses] = useState(courses);

  const t = translations[language];

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 24);
    window.addEventListener('scroll', onScroll, { passive: true });
    const observer = new IntersectionObserver((entries) => entries.forEach((entry) => entry.isIntersecting && entry.target.classList.add('visible')), { threshold: .12 });
    document.querySelectorAll('.reveal').forEach((el) => observer.observe(el));
    const sectionObserver = new IntersectionObserver((entries) => entries.forEach((entry) => entry.isIntersecting && setActive(entry.target.id)), { rootMargin: '-20% 0px -70% 0px' });
    navIds.forEach((id) => { const element = document.getElementById(id); if (element) sectionObserver.observe(element); });
    
    // Fetch real data from Supabase
    const fetchDbData = async () => {
      // Usamos import dinámico para evitar error si el archivo no exporta la funcion o se rompe la build
      const { getSupabaseClient } = await import('@/lib/supabase/client');
      const supabase = getSupabaseClient();
      if (!supabase) return; // Se queda con los mocks

      try {
        const { data: profs } = await supabase.from('profesionales').select('especialidad, descripcion, usuarios(nombre, foto)').eq('estado', 'activo').limit(3);
        if (profs && profs.length > 0) {
          setDbProfessionals(profs.map((p) => {
            const usuario = (p as unknown as { usuarios?: { nombre: string | null; foto: string | null }[] | null }).usuarios?.[0];
            return {
              name: usuario?.nombre || 'Profesional',
              specialty: p.especialidad || 'Psicología',
              description: p.descripcion || '',
              modality: 'Online y presencial',
              image: usuario?.foto || images.professionalOne
            };
          }));
        }

        const { data: cur } = await supabase.from('cursos').select('*').eq('estado', 'publicado').limit(3);
        if (cur && cur.length > 0) {
          setDbCourses(cur.map((c) => ({
            title: c.nombre,
            category: 'CURSO',
            description: c.descripcion || '',
            duration: 'A tu ritmo',
            modality: 'Online',
            image: c.imagen || images.courseOne
          })));
        }
      } catch (e) {
        console.error('Error fetching data from Supabase:', e);
      }
    };
    fetchDbData();

    return () => { window.removeEventListener('scroll', onScroll); observer.disconnect(); sectionObserver.disconnect(); };
  }, []);

  const services = useMemo(() => [
    { title: t.individual, description: language === 'es' ? 'Un espacio para escucharte y volver a ti.' : 'A space to listen to yourself and return to you.', color: 'bg-brand-600', icon: serviceIcons[0] },
    { title: t.couple, description: language === 'es' ? 'Para cuidar el vínculo desde la comprensión.' : 'To care for the bond through understanding.', color: 'bg-brand-50', icon: serviceIcons[1] },
    { title: t.family, description: language === 'es' ? 'Nuevas formas de encontrarse en familia.' : 'New ways of reconnecting as a family.', color: 'bg-white', icon: serviceIcons[2] },
  ], [language, t]);

  const validateForm = () => form.name.trim() && /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email) && form.subject.trim() && form.body.trim() && form.privacy;
  const submitForm = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!validateForm()) return; setSending(true); window.setTimeout(() => { setSending(false); setSubmitted(true); }, 900); };
  const scrollTo = (id: string) => { setMenuOpen(false); document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' }); };

  return <div id="top" className="overflow-hidden bg-white">
    <header className={`fixed inset-x-0 top-0 z-50 transition-all duration-300 ${scrolled ? 'bg-white/90 shadow-[0_8px_30px_rgba(45,72,95,.08)] backdrop-blur-lg' : 'bg-white/75 backdrop-blur-sm'}`}>
      <div className="container-wide flex h-[82px] items-center justify-between gap-5 transition-all duration-300 sm:h-[86px]">
        <a href="#top" aria-label="Psique Amor" className="focus-ring shrink-0"><img src={logo} alt="Psique Amor" className="w-[106px] sm:w-[124px]" /></a>
        <nav className="hidden items-center gap-5 lg:flex" aria-label="Primary navigation">{t.nav.map((item, i) => { const cls = `focus-ring relative py-3 text-[12px] font-semibold transition-colors after:absolute after:bottom-0 after:left-0 after:h-px after:bg-brand-600 after:transition-all ${active === navIds[i] ? 'text-brand-700 after:w-full' : 'text-ink/65 after:w-0 hover:text-brand-600 hover:after:w-full'}`; return navRoutes[i] ? <Link key={item} to={navRoutes[i]} className={cls}>{item}</Link> : <a key={item} href={`#${navIds[i]}`} className={cls}>{item}</a>; })}</nav>
        <div className="hidden items-center gap-3 lg:flex"><div className="flex gap-1 rounded-full bg-brand-50 p-1 text-[10px] font-bold"><button onClick={() => setLanguage('es')} className={`rounded-full px-2 py-1 ${language === 'es' ? 'bg-white text-brand-700 shadow-sm' : 'text-ink/45'}`}>ES</button><button onClick={() => setLanguage('en')} className={`rounded-full px-2 py-1 ${language === 'en' ? 'bg-white text-brand-700 shadow-sm' : 'text-ink/45'}`}>EN</button></div><Link to="/iniciar-sesion" className="text-xs font-bold text-ink/65 hover:text-brand-600">{language === 'es' ? 'Iniciar sesión' : 'Log in'}</Link><AppButton href="/agendar">{t.primary}<ArrowRight size={15} /></AppButton></div>
        <div className="flex items-center gap-2 lg:hidden"><div className="flex gap-1 rounded-full bg-brand-50 p-1 text-[10px] font-bold"><button onClick={() => setLanguage('es')} className={`rounded-full px-2 py-1 ${language === 'es' ? 'bg-white text-brand-700 shadow-sm' : 'text-ink/45'}`}>ES</button><button onClick={() => setLanguage('en')} className={`rounded-full px-2 py-1 ${language === 'en' ? 'bg-white text-brand-700 shadow-sm' : 'text-ink/45'}`}>EN</button></div><button onClick={() => setMenuOpen(!menuOpen)} className="focus-ring grid h-11 w-11 place-items-center rounded-full bg-brand-50 text-brand-700" aria-label={menuOpen ? 'Close menu' : 'Open menu'}>{menuOpen ? <X size={20} /> : <Menu size={20} />}</button></div>
      </div>
      <div className={`lg:hidden ${menuOpen ? 'max-h-[460px] border-t border-brand-100' : 'max-h-0'} overflow-hidden bg-white transition-all duration-500`}><nav className="container-wide flex flex-col gap-1 py-4">{t.nav.map((item, i) => navRoutes[i] ? <Link key={item} to={navRoutes[i]} onClick={() => setMenuOpen(false)} className="focus-ring rounded-xl px-4 py-3 text-sm font-semibold text-ink/75 hover:bg-brand-50 hover:text-brand-700">{item}</Link> : <a key={item} onClick={() => scrollTo(navIds[i])} className="focus-ring rounded-xl px-4 py-3 text-sm font-semibold text-ink/75 hover:bg-brand-50 hover:text-brand-700">{item}</a>)}<div className="mt-3 flex flex-wrap gap-3 border-t border-brand-100 pt-4"><Link to="/iniciar-sesion" onClick={() => setMenuOpen(false)} className="rounded-full border border-brand-300 px-4 py-3 text-sm font-bold text-brand-700">{language === 'es' ? 'Iniciar sesión' : 'Log in'}</Link><AppButton href="/agendar" onClick={() => setMenuOpen(false)}>{t.primary}<ArrowRight size={15} /></AppButton></div></nav></div>
    </header>

    <main>
      <section className="relative isolate min-h-[760px] overflow-hidden bg-mist-gradient pt-32 sm:pt-36 lg:min-h-[820px] lg:pt-44"><div className="pointer-events-none absolute -right-40 top-20 -z-10 h-[620px] w-[620px] rounded-full bg-brand-200/40 blur-3xl" /><div className="pointer-events-none absolute left-[45%] top-24 -z-10 h-72 w-72 rounded-full bg-lilac-300/30 blur-3xl" /><svg className="pointer-events-none absolute right-0 top-24 -z-10 h-[500px] w-[56%] opacity-30" viewBox="0 0 700 500" fill="none"><path d="M-30 280C120 40 325 34 520 140C660 216 720 176 750 90" stroke="#5d83a7" strokeWidth="1.5" /><path d="M-20 340C150 100 360 110 515 200C640 274 700 247 740 188" stroke="#9580b9" strokeWidth="1.5" /><path d="M140 490C250 280 420 260 570 320C650 352 695 330 740 290" stroke="#5d83a7" strokeWidth="1" /></svg><div className="container-wide grid items-center gap-12 lg:grid-cols-[.84fr_1.16fr] lg:gap-6"><div className="reveal relative z-10 max-w-xl"><span className="eyebrow">{t.heroLabel}</span><h1 className="mt-6 text-[clamp(3rem,6.4vw,5.7rem)] font-semibold tracking-[-.045em] text-ink">{t.heroTitleBefore}<span className="text-brand-600">{t.heroTitleBlue}</span>{t.heroTitleMiddle}<span className="text-lilac-600">{t.heroTitlePurple}</span></h1><p className="mt-7 max-w-lg text-base leading-7 text-ink/65 sm:text-lg">{t.heroText}</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><AppButton href="/agendar"><CalendarDays size={17} />{t.primary}<ArrowRight size={15} /></AppButton><AppButton href="/servicios" outline>{t.servicesLink}<ArrowRight size={15} /></AppButton></div><div className="mt-10 flex flex-wrap gap-x-6 gap-y-3 text-xs font-semibold text-ink/60"><span className="flex items-center gap-2"><Check className="text-brand-500" size={15} />{language === 'es' ? 'Atención personalizada' : 'Personal attention'}</span><span className="flex items-center gap-2"><Check className="text-brand-500" size={15} />{language === 'es' ? 'Confidencialidad' : 'Confidentiality'}</span><span className="flex items-center gap-2"><Check className="text-brand-500" size={15} />{language === 'es' ? 'Online y presencial' : 'Online and in-person'}</span></div></div><div className="reveal relative mx-auto w-full max-w-[600px] lg:ml-auto" style={{ transitionDelay: '120ms' }}><div className="absolute -inset-4 rounded-[40%_60%_50%_45%] bg-brand-500/80 blur-[1px] sm:-inset-8" /><div className="absolute -bottom-10 -left-8 h-40 w-40 rounded-full bg-lilac-400/80 sm:-left-16" /><div className="relative aspect-[.9/1] overflow-hidden rounded-[45%_35%_42%_28%] border-[10px] border-white/50 shadow-lift sm:aspect-[1/1.03]"><img src={images.hero} alt="Psicóloga conversando en un espacio cálido" className="h-full w-full object-cover" /></div><div className="absolute -left-2 top-10 max-w-[220px] rounded-3xl bg-white/85 p-4 shadow-soft backdrop-blur-md sm:-left-10 sm:top-16"><Heart className="mb-3 text-lilac-500" size={19} fill="currentColor" /><p className="font-display text-lg leading-tight text-ink">{language === 'es' ? 'No tienes que tener todas las respuestas para comenzar.' : 'You do not need all the answers to begin.'}</p></div><div className="absolute -bottom-5 right-2 flex items-center gap-2 rounded-full bg-brand-700 px-4 py-3 text-sm font-bold text-white shadow-soft sm:-right-8"><Sparkles size={16} />{language === 'es' ? 'Un espacio para ti' : 'A space for you'}</div></div></div></section>

      <section className="relative z-10 -mt-7"><div className="container-wide"><div className="grid rounded-3xl bg-white p-2 shadow-soft sm:grid-cols-2 lg:grid-cols-4">{t.trust.map((item, i) => { const Icon = trustIcons[i]; return <div key={item} className={`flex items-center gap-3 px-4 py-5 sm:px-6 lg:justify-center ${i < 3 ? 'lg:border-r lg:border-brand-100' : ''}`}><Icon className="shrink-0 text-brand-500" size={22} strokeWidth={1.5} /><span className="text-sm font-semibold text-ink/70">{item}</span></div>; })}</div></div></section>

      <section id="services" className="section-space"><div className="container-wide"><div className="reveal flex flex-col justify-between gap-8 lg:flex-row lg:items-end"><SectionHeading label={t.servicesLabel} title={t.servicesTitle} text={t.servicesText} /><Link to="/servicios" className="group inline-flex shrink-0 items-center gap-2 text-sm font-bold text-brand-600">{t.allServices}<ArrowRight size={16} className="transition-transform group-hover:translate-x-1" /></Link></div><div className="mt-14 grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><div className="reveal group relative min-h-[390px] overflow-hidden rounded-organic bg-brand-600 p-8 text-white shadow-soft transition duration-500 hover:-translate-y-1 sm:p-12"><div className="absolute -right-16 -top-16 h-64 w-64 rounded-full border-[28px] border-white/10" /><div className="absolute bottom-[-70px] right-8 h-44 w-44 rounded-full bg-brand-400/40 blur-2xl" /><Heart className="relative mb-20" size={45} strokeWidth={1.3} /><div className="relative max-w-md"><h3 className="text-3xl font-semibold">{services[0].title}</h3><p className="mt-3 max-w-sm leading-7 text-white/70">{services[0].description}</p><a href="#contact" className="group/link mt-7 inline-flex items-center gap-3 text-sm font-bold">{t.learn}<ArrowRight className="transition-transform group-hover/link:translate-x-1" size={16} /></a></div></div><div className="grid gap-5"><ServiceCard service={services[1]} action={t.learn} /><ServiceCard service={services[2]} action={t.learn} /></div></div></div></section>

      <section className="section-space bg-brand-50/80"><div className="container-wide grid items-center gap-14 lg:grid-cols-[.85fr_1.15fr] lg:gap-24"><div className="reveal relative mx-auto w-full max-w-[440px]"><div className="absolute -left-8 top-10 h-56 w-56 rounded-full border border-lilac-400/50" /><div className="absolute -bottom-6 -right-6 h-44 w-44 rounded-full bg-lilac-200/80" /><div className="relative h-[460px] overflow-hidden rounded-[48%_35%_44%_32%] shadow-soft"><img src={images.emotional} alt="Mujer en calma junto a una ventana" className="h-full w-full object-cover" /></div><div className="absolute -bottom-5 left-4 grid h-20 w-20 place-items-center rounded-full border-8 border-brand-50 bg-brand-600 text-white shadow-soft"><Heart size={26} /></div></div><div className="reveal"><SectionHeading label={t.emotionalLabel} title={t.emotionalTitle} text={t.emotionalText} /><ul className="mt-8 space-y-5">{t.points.map((point) => <li key={point} className="flex items-center gap-4 text-sm font-semibold text-ink/75"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-white text-brand-600 shadow-sm"><Check size={15} /></span>{point}</li>)}</ul><AppButton href="#contact" className="mt-9">{t.about}<ArrowRight size={15} /></AppButton></div></div></section>

      <section id="professionals" className="section-space"><div className="container-wide"><div className="reveal flex flex-col justify-between gap-8 lg:flex-row lg:items-end"><SectionHeading label={t.teamLabel} title={t.teamTitle} text={t.teamText} /><Link to="/profesionales" className="focus-ring inline-flex min-h-12 items-center justify-center gap-3 rounded-full border border-brand-300 bg-white/50 px-6 text-sm font-bold text-brand-700 transition duration-300 hover:-translate-y-1 hover:border-brand-500 hover:bg-white">{t.teamButton}<ArrowRight size={15} /></Link></div><div className="mt-14 grid gap-6 md:grid-cols-3">{dbProfessionals.map((person, i) => <article key={person.name} className={`reveal group overflow-hidden rounded-[28px] bg-white shadow-soft transition duration-500 hover:-translate-y-2 ${i === 1 ? 'md:translate-y-8' : ''}`} style={{ transitionDelay: `${i * 80}ms` }}><div className="relative aspect-[.78/1] overflow-hidden"><img src={person.image} alt={person.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.03]" /><div className="absolute inset-0 bg-brand-700/0 transition duration-500 group-hover:bg-brand-700/20" /><span className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-2 text-[10px] font-bold uppercase tracking-wider text-brand-700">{person.specialty}</span><span className="absolute bottom-4 right-4 grid h-10 w-10 translate-y-3 place-items-center rounded-full bg-white text-brand-700 opacity-0 shadow-soft transition duration-300 group-hover:translate-y-0 group-hover:opacity-100"><ArrowUpRight size={17} /></span></div><div className="p-5"><h3 className="text-2xl font-semibold text-ink">{person.name}</h3><p className="mt-2 text-sm text-ink/60">{person.description}</p><div className="mt-5 flex items-center justify-between border-t border-brand-100 pt-4 text-xs font-semibold text-ink/55"><span>{person.modality}</span><a href="#contact" className="text-brand-600">{t.profile}</a></div></div></article>)}</div></div></section>
  
      <section id="courses" className="section-space bg-brand-900 text-white"><div className="container-wide"><div className="reveal flex flex-col justify-between gap-8 lg:flex-row lg:items-end"><SectionHeading light label={t.coursesLabel} title={t.coursesTitle} text={t.coursesText} /><Link to="/cursos" className="group inline-flex shrink-0 items-center gap-2 text-sm font-bold text-white">{t.exploreCourses}<ArrowRight size={16} className="transition-transform group-hover:translate-x-1" /></Link></div><div className="mt-14 grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><CourseCard course={dbCourses[0]} large /><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-1">{dbCourses.slice(1).map((course) => <CourseCard key={course.title} course={course} />)}</div></div></div></section>

      <section id="resources" className="section-space"><div className="container-wide"><div className="reveal flex flex-col justify-between gap-8 lg:flex-row lg:items-end"><SectionHeading label={t.resourcesLabel} title={t.resourcesTitle} text={t.resourcesText} /><Link to="/recursos" className="group inline-flex shrink-0 items-center gap-2 text-sm font-bold text-brand-600">{t.exploreResources}<ArrowRight size={16} className="transition-transform group-hover:translate-x-1" /></Link></div><div className="mt-14 grid gap-6 lg:grid-cols-[1.25fr_.75fr]"><article className="reveal group relative min-h-[420px] overflow-hidden rounded-[32px] p-7 text-white sm:p-10"><img src={images.video} alt="Mujer escribiendo en un momento de autocuidado" className="absolute inset-0 h-full w-full object-cover transition duration-700 group-hover:scale-[1.03]" /><div className="absolute inset-0 bg-gradient-to-t from-brand-900 via-brand-900/30 to-brand-700/10" /><div className="relative flex h-full flex-col justify-between"><div className="flex items-start justify-between"><span className="rounded-full border border-white/30 bg-white/10 px-3 py-2 text-[10px] font-bold uppercase tracking-wider backdrop-blur-sm">{t.videoTag}</span><button aria-label={t.play} className="grid h-16 w-16 animate-pulse place-items-center rounded-full bg-white text-brand-700 shadow-soft"><Play size={22} fill="currentColor" /></button></div><div><h3 className="max-w-lg text-3xl font-semibold sm:text-4xl">{t.videoTitle}</h3><p className="mt-3 max-w-md text-sm leading-6 text-white/75">{language === 'es' ? 'Pequeños límites pueden hacer más espacio para lo que importa.' : 'Small boundaries can make more room for what matters.'}</p><a href="#resources" className="mt-6 inline-flex items-center gap-2 text-sm font-bold">{t.play}<ArrowRight size={16} /></a></div></div></article><article className="reveal relative overflow-hidden rounded-[32px] bg-brand-50 p-7 sm:p-10"><div className="absolute -right-10 top-10 h-48 w-48 rounded-full bg-lilac-200/80" /><div className="relative"><span className="text-[10px] font-bold uppercase tracking-[.2em] text-lilac-600">{t.bookTag}</span><div className="my-8 flex justify-center"><div className="relative h-44 w-32 -rotate-6 rounded-r-lg bg-brand-700 p-5 text-white shadow-lift"><div className="absolute inset-y-0 left-0 w-2 bg-lilac-400" /><BookOpen className="mb-5" size={20} /><p className="font-display text-xl leading-tight">{t.bookTitle}</p></div></div><h3 className="text-3xl font-semibold text-ink">{t.bookTitle}</h3><p className="mt-3 text-sm leading-6 text-ink/60">{language === 'es' ? 'Una guía amable para cerrar ciclos y volver a empezar.' : 'A gentle guide to closing cycles and beginning again.'}</p><a href="#contact" className="group mt-6 inline-flex items-center gap-2 text-sm font-bold text-brand-600">{t.download}<ArrowRight size={16} className="transition-transform group-hover:translate-x-1" /></a></div></article></div></div></section>

      <section className="section-space bg-mist-gradient"><div className="container-wide"><div className="reveal text-center"><SectionHeading label={t.benefitsLabel} title={t.benefitsTitle} /></div><div className="relative mt-16 grid gap-10 md:grid-cols-5 md:gap-4"><div className="absolute left-[10%] right-[10%] top-8 hidden h-px bg-brand-300 md:block" />{t.benefits.map(([title, body], i) => { const Icon = benefitIcons[i]; return <div key={title} className={`reveal relative z-10 text-center ${i % 2 === 1 ? 'md:translate-y-8' : ''}`} style={{ transitionDelay: `${i * 70}ms` }}><div className="mx-auto grid h-16 w-16 place-items-center rounded-full border-8 border-brand-100 bg-white text-brand-600 shadow-soft"><Icon size={21} strokeWidth={1.5} /></div><h3 className="mt-5 text-xl font-semibold text-ink">{title}</h3><p className="mx-auto mt-2 max-w-[170px] text-sm leading-5 text-ink/60">{body}</p></div>; })}</div></div></section>

      <section className="section-space relative overflow-hidden bg-brand-gradient text-white"><div className="pointer-events-none absolute -left-20 -top-32 h-96 w-96 rounded-full border-[70px] border-white/10" /><div className="pointer-events-none absolute -right-20 bottom-[-150px] h-[500px] w-[500px] rounded-full bg-white/10 blur-3xl" /><div className="container-wide relative grid items-center gap-12 lg:grid-cols-[1.1fr_.9fr]"><div className="reveal"><span className="eyebrow text-white/80">{t.ctaLabel}</span><h2 className="mt-5 max-w-2xl text-4xl font-semibold sm:text-6xl">{t.ctaTitle}</h2><p className="mt-6 max-w-lg text-base leading-7 text-white/75">{t.ctaText}</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><AppButton href="/agendar" light>{t.primary}<ArrowRight size={15} /></AppButton><a href={contactConfig.whatsapp} className="focus-ring inline-flex min-h-12 items-center justify-center gap-3 rounded-full border border-white/30 px-6 text-sm font-bold text-white transition hover:bg-white/10"><MessageCircle size={17} className="text-[#25D366]" />{t.whatsapp}</a></div></div><div className="reveal relative mx-auto max-w-sm"><div className="absolute -inset-6 rounded-[50%] bg-white/10 blur-2xl" /><img src={images.cta} alt="Persona escribiendo en calma" className="relative aspect-square w-full rounded-[42%_32%_45%_28%] object-cover shadow-lift" /></div></div></section>

      <section id="contact" className="section-space"><div className="container-wide"><div className="reveal"><SectionHeading label={t.contactLabel} title={t.contactTitle} text={t.contactText} /></div><div className="mt-14 grid gap-6 lg:grid-cols-[.72fr_1.28fr]"><aside className="reveal relative overflow-hidden rounded-[32px] bg-brand-700 p-8 text-white sm:p-10"><div className="absolute -bottom-16 -right-16 h-64 w-64 rounded-full border-[35px] border-white/10" /><div className="relative"><div className="mb-16 inline-flex rounded-xl bg-white px-4 py-3"><img src={logo} alt="Psique Amor" className="w-[150px]" /></div><p className="max-w-xs font-display text-2xl leading-snug">{t.message}</p><div className="mt-12 space-y-5 text-sm text-white/75"><a href={`mailto:${contactConfig.email}`} className="flex items-center gap-3"><Mail size={17} />{contactConfig.email}</a><a href={contactConfig.whatsapp} className="flex items-center gap-3"><MessageCircle size={17} />{contactConfig.phone}</a><div className="flex items-center gap-3"><MapPin size={17} />{contactConfig.location}</div><div className="flex items-center gap-3"><Clock3 size={17} />{contactConfig.hours}</div></div></div></aside><form onSubmit={submitForm} className="reveal rounded-[32px] bg-brand-50 p-7 sm:p-10"><div className="grid gap-5 sm:grid-cols-2"><Field label={t.form.name} value={form.name} onChange={(value) => setForm({ ...form, name: value })} /><Field label={t.form.email} type="email" value={form.email} onChange={(value) => setForm({ ...form, email: value })} /><Field label={t.form.subject} value={form.subject} onChange={(value) => setForm({ ...form, subject: value })} /><div className="hidden sm:block" /><Field label={t.form.body} value={form.body} onChange={(value) => setForm({ ...form, body: value })} textarea /><label className="sm:col-span-2 flex cursor-pointer items-start gap-3 text-sm text-ink/65"><input type="checkbox" checked={form.privacy} onChange={(e) => setForm({ ...form, privacy: e.target.checked })} className="mt-1 h-4 w-4 accent-brand-600" />{t.form.privacy}</label></div><div className="mt-7 flex flex-col items-start gap-4 sm:flex-row sm:items-center"><button type="submit" disabled={!validateForm() || sending || submitted} className="focus-ring inline-flex min-h-12 items-center justify-center gap-3 rounded-full bg-brand-gradient px-7 text-sm font-bold text-white shadow-soft transition hover:-translate-y-1 disabled:cursor-not-allowed disabled:opacity-45">{sending ? t.form.sending : submitted ? <><Check size={16} />{t.form.success}</> : <>{t.form.send}<ArrowRight size={15} /></>}</button>{submitted && <span className="text-sm font-semibold text-brand-600">{t.form.success}</span>}</div></form></div></div></section>
    </main>

    <footer className="bg-brand-900 text-white"><div className="container-wide py-16 sm:py-20"><div className="grid gap-12 lg:grid-cols-[1.5fr_1fr_1fr_1fr_1fr]"><div><div className="inline-flex rounded-xl bg-white px-4 py-3"><img src={logo} alt="Psique Amor" className="w-[168px]" /></div><p className="mt-6 max-w-xs text-sm leading-6 text-white/60">{language === 'es' ? 'Un espacio seguro para volver a encontrarte.' : 'A safe space to find your way back to yourself.'}</p><div className="mt-7 flex gap-3"><a href="#top" aria-label="Instagram" className="grid h-10 w-10 place-items-center rounded-full border border-white/15 text-white/70 transition hover:bg-white/10 hover:text-white"><Instagram size={16} /></a><a href="#top" aria-label="LinkedIn" className="grid h-10 w-10 place-items-center rounded-full border border-white/15 text-white/70 transition hover:bg-white/10 hover:text-white"><Linkedin size={16} /></a><a href="#top" aria-label="Youtube" className="grid h-10 w-10 place-items-center rounded-full border border-white/15 text-white/70 transition hover:bg-white/10 hover:text-white"><Youtube size={16} /></a></div></div><FooterColumn title={t.footerNav} links={t.nav} /><FooterColumn title={t.footerServices} links={[t.individual, t.couple, t.family, language === 'es' ? 'Orientación emocional' : 'Emotional guidance', language === 'es' ? 'Sesiones online' : 'Online sessions']} /><FooterColumn title={t.footerResources} links={[language === 'es' ? 'Cursos' : 'Courses', language === 'es' ? 'Videos' : 'Videos', language === 'es' ? 'Libros digitales' : 'Digital books', 'Aula virtual']} /><div><h3 className="text-sm font-bold">{t.footerContact}</h3><div className="mt-5 space-y-3 text-sm leading-6 text-white/60"><p>{contactConfig.phone}</p><p>{contactConfig.email}</p><p>{contactConfig.location}</p><p>{contactConfig.hours}</p></div></div></div><div className="mt-14 flex flex-col justify-between gap-4 border-t border-white/10 pt-6 text-xs text-white/45 sm:flex-row"><span>© 2024 Psique Amor</span><div className="flex flex-wrap gap-5"><a href="#contact" className="hover:text-white">{t.privacy}</a><a href="#contact" className="hover:text-white">{t.terms}</a><span>ES | EN</span></div></div></div></footer>
    <a href={contactConfig.whatsapp} aria-label={t.talk} className="group fixed bottom-5 right-5 z-40 grid h-14 w-14 place-items-center rounded-full border-4 border-white bg-[#25D366] text-white shadow-lift transition hover:scale-105 sm:bottom-7 sm:right-7"><MessageCircle size={25} fill="currentColor" /><span className="pointer-events-none absolute right-16 whitespace-nowrap rounded-full bg-brand-900 px-3 py-2 text-xs font-bold opacity-0 shadow-soft transition group-hover:opacity-100">{t.talk}</span></a>
  </div>;
}

function ServiceCard({ service, action }: { service: { title: string; description: string; color: string; icon: typeof Heart }; action: string }) { const Icon = service.icon; return <a href="#contact" className={`group relative flex min-h-[185px] flex-col justify-between overflow-hidden rounded-[28px] p-7 transition duration-500 hover:-translate-y-1 hover:shadow-soft ${service.color} ${service.color === 'bg-white' ? 'border border-brand-100' : ''}`}><Icon className={service.color === 'bg-brand-50' ? 'text-lilac-500' : 'text-brand-600'} size={29} strokeWidth={1.4} /><div><h3 className="text-2xl font-semibold text-ink">{service.title}</h3><p className="mt-2 max-w-sm text-sm leading-6 text-ink/60">{service.description}</p><span className="mt-4 inline-flex items-center gap-2 text-xs font-bold text-brand-600">{action}<ArrowRight size={14} className="transition-transform group-hover:translate-x-1" /></span></div></a>; }
function CourseCard({ course, large = false }: { course: typeof courses[number]; large?: boolean }) { return <a href="#contact" className={`group relative overflow-hidden rounded-[28px] border border-white/10 ${large ? 'min-h-[390px]' : 'min-h-[220px]'}`}><img src={course.image} alt={course.title} className="absolute inset-0 h-full w-full object-cover transition duration-700 group-hover:scale-[1.04]" /><div className="absolute inset-0 bg-gradient-to-t from-brand-900 via-brand-900/65 to-brand-900/5 transition group-hover:via-brand-900/75" /><div className="relative flex h-full flex-col justify-end p-6 sm:p-8"><span className="text-[10px] font-bold uppercase tracking-[.2em] text-brand-200">{course.category}</span><h3 className={`mt-2 font-display font-semibold ${large ? 'text-3xl sm:text-4xl' : 'text-2xl'}`}>{course.title}</h3><p className="mt-2 max-w-lg text-sm leading-5 text-white/70">{course.description}</p><div className="mt-5 flex items-center justify-between text-xs font-semibold text-white/60"><span>{course.duration} · {course.modality}</span><span className="grid h-9 w-9 place-items-center rounded-full bg-white/15 text-white transition group-hover:bg-white group-hover:text-brand-700"><MoveRight size={16} /></span></div></div></a>; }
function Field({ label, value, onChange, type = 'text', textarea = false }: { label: string; value: string; onChange: (value: string) => void; type?: string; textarea?: boolean }) { const common = 'mt-2 w-full rounded-2xl border border-transparent bg-white px-4 py-3 text-sm text-ink outline-none transition placeholder:text-ink/30 focus:border-brand-300 focus:ring-4 focus:ring-brand-100'; return <label className={textarea ? 'sm:col-span-2' : ''}><span className="text-xs font-bold text-ink/70">{label}</span>{textarea ? <textarea rows={4} value={value} onChange={(e) => onChange(e.target.value)} className={common} /> : <input type={type} value={value} onChange={(e) => onChange(e.target.value)} className={common} />}</label>; }
function FooterColumn({ title, links }: { title: string; links: readonly string[] }) { return <div><h3 className="text-sm font-bold">{title}</h3><ul className="mt-5 space-y-3 text-sm text-white/60">{links.map((link) => <li key={link}><a href="#top" className="transition hover:text-white">{link}</a></li>)}</ul></div>; }
